import Image from "@geist-ui/icons/image";
import Bold from "@geist-ui/icons/bold";
import Italic from "@geist-ui/icons/italic";
import Code from "@geist-ui/icons/code";
import List from "@geist-ui/icons/list";

export const ImageIcon = (props) => <Image {...props} />;

export const BoldIcon = (props) => <Bold {...props} />;

export const ItalicIcon = (props) => <Italic {...props} />;

export const CodeIcon = (props) => <Code {...props} />;

export const ListIcon = (props) => <List {...props} />;
